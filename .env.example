# Environment variables for the poll application
# Copy this file to .env and fill in your values

# JWT Secret for authentication (generate a strong random string)
JWT_SHARED_SECRET=your-very-secure-jwt-secret-key-here

# Domain configuration for Traefik (update with your domain)
DOMAIN=your-domain.com

# Database configuration
NODE_ENV=production
